{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parties list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Artist</th>
                <th>Place</th>
                <th>Content</th>
                <th>Souncloud</th>
                <th>Keyword</th>
                <th>Imgslide</th>
                <th>Imgcover</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for party in parties %}
            <tr>
                <td><a href="{{ path('parties_show', { 'id': party.id }) }}">{{ party.id }}</a></td>
                <td>{{ party.title }}</td>
                <td>{{ party.artist }}</td>
                <td>{{ party.place }}</td>
                <td>{{ party.content }}</td>
                <td>{{ party.souncloud }}</td>
                <td>{{ party.keyword }}</td>
                <td>{{ party.imgSlide }}</td>
                <td>{{ party.imgCover }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('parties_show', { 'id': party.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('parties_edit', { 'id': party.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('parties_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
