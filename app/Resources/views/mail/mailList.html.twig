{% extends 'baseAdmin.html.twig' %}

{% block content %}
    <a href="{{ path('mail_index') }}">Back to the list</a>
    <p>
        {% for mail in emails %}
            {{ mail.mail }};
        {% endfor %}
    </p>


{% endblock %}